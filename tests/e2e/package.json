{
  "name": "cotaobra-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "CotaObra — Playwright smoke E2E (5 cenários P0 do Sprint 0)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^20.12.0"
  }
}
